<label
    class="label"
    for="{{name}}"
>
    {{title}}
</label>
<div class="has-icons-left has-icons-right">
    <input
        name="{{name}}"
        class="input"
        placeholder="{{placeholder}}"
        value="{{value}}"
        @if(name === 'email')
            type="text"
        @else
            type="password"
        @end
        
        @if(active === false)
            disabled
        @end
    >
</div>
@if(flashMessages.has(`errors.${name}`) && type === 'create')
    <div
        class="error"
        x-data="{click: false}"
        x-show="!click"
        x-transition.opacity.duration.1200ms
    >
        <span
            class="subtitle is-6 has-text-danger"
            x-on:click="click = !click"
        >
            {{ flashMessages.get(`errors.${name}`) }}
        </span>
        <img
            src="/icons/circle-xmark-solid.svg"
            class="exit-icon"
            x-on:click="click = !click"
        />
    </div>
@end

@if(flashMessages.has('success'))
    <div
        class="flash--cont has-text-centered"
        
    >
        <button
            class="flash success"
            x-data="{click: false}"
            x-show="!click"
            x-on:click="click = !click"
            x-transition
        >
            {{ flashMessages.get('success') }}
        </button>
    </div>
@end
@layout('layout')

@section('page')

    <section
        class="section is-large has-text-centered centering-item blog"
    >
        <h1
            class="title"
        >
            Derniers articles
        </h1>

        <ul
            class="columns is-multiline"
        >
            @each(post in posts)
                <li class="column is-narrow">
                    <a href="{{route('posts.article', {id: post.id})}}">
                        <div class="card is-white add-blur">
                            <div class="card-header">
                                <h3
                                    class=" title is-5"
                                    style="height: 48px;"
                                >
                                    {{post.title}}
                                </h3>
                            </div>
                            <div class=card-content>
                                {{post.recap}}    
                            </div>
                            <div class="card-footer">{{post.createdAt}}</div>
                        </div>
                    </a>
                </li>
            @endeach
        </ul>
    </section>

    <section class="section has-text-centered  is-medium is-">
        <div class="container">
            <h2 class="title">Newsletter</h2>

            <span class="content">Suivez ma nouvelle lettre</span>
        </div>
    </section>
@endsection